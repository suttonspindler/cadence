# Production dependencies for the AI service (Render free tier).
#
# Deliberately omits `sentence-transformers` (and its torch dependency, ~1GB):
# in production EMBEDDINGS_PROVIDER=voyage, and the local sentence-transformers
# model is imported lazily (only on the "local" path), so it's never loaded.
# Dropping it keeps the build fast and the container small enough for 512MB RAM.
# For local development install requirements.txt instead (includes the model).
fastapi>=0.115
uvicorn[standard]>=0.32
psycopg[binary]>=3.2
psycopg-pool>=3.2
pgvector>=0.3.6
numpy>=2.0
httpx>=0.27
pydantic-settings>=2.5
anthropic>=0.40
